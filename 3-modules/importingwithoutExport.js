const num1 = 5
const num2 = 10

function addValues() {
  console.log(`the sum is ${num1 + num2}`)
}

addValues() //this function will run in app.js if we used require in app.js even without asigning it to a varibale and without using module.exports